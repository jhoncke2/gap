import 'package:flutter/material.dart';
class FirstFirmerInformation extends StatelessWidget {
  const FirstFirmerInformation({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('First firmer information'),
    );
  }
}