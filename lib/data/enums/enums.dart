enum NetConnectionState{
  Connected,
  Disonnected
}

enum DataSource{
  Storage,
  Services
}

enum ProcessStage{
  Pendiente,
  Realizada
}

enum BlocName{
  Projects,
  Visits,
  Formularios,
  ChosenForm,
  Images,
  CommentedImages,
  FirmPaint,
  Index
}